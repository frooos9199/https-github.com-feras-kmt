{
  "framework": "nextjs",
  "buildCommand": "npm install && npm run build",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  },
  "api": {
    "bodyParser": {
      "sizeLimit": "50mb"
    }
  }
}
